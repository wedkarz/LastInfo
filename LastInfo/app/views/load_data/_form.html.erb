<%= form_for(@load_datum) do |f| %>
  <% if @load_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@load_datum.errors.count, "error") %> prohibited this load_datum from being saved:</h2>

      <ul>
      <% @load_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
